if git diff | grep '"execution_count": [0-9]' | ls > /dev/null; then
  echo;
  echo 'ERROR';
  echo 'Please clear all outputs from your Jupyter notebooks before committing.';
  echo;
  exit 1;
else
  exit 0;
fi
